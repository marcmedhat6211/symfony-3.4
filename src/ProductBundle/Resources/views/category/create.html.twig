{% extends('base.html.twig') %}

{% block body %}
    <br>
    <h2 style="text-align: center;">{{'Create Category' | trans}}</h2>
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}